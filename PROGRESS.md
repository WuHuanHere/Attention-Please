# 进度与试跑须知

> 这份文件是给"三天后的你"看的:如果出了怪事,先看这里的**已知问题**和**实测数据**,
> 能省掉一大堆重复排查。

## 一、当前状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | 环境/摄像头选型/帧率CPU/离线/占用实测 | ✅ 完成 |
| M1 | 交互校准 → `calibration.json` | ✅ 完成(你的阈值已生成) |
| M2 | 纯逻辑单测(状态机/时间表/白黑名单/分级/日报/托盘) | ✅ 完成(**120 项全绿**) |
| M3 | 切窗口信号 → 分级提醒 → 入库 | ✅ 完成,端到端实测通过,试跑中 |
| M4 | 看手机 + 离座 + **Pose 弱证据** | ✅ 看手机/离座/Pose 均已启用并真机验证;**真人标定已完成**;**发呆已砍掉**(见七之十三) |
| M5 | 分心截图拼接 + 7 天清理 + 21:30 日报 | ✅ 完成 |
| M6 | 托盘 + 暂停写理由 + 开机自启 | ✅ 完成,**已真人验证通过** |

## 二、实测数据(都是本机真实测出来的,不是估算)

| 指标 | 数值 | 说明 |
|---|---|---|
| 采集分辨率 | 1280x720, 摄像头自给 ~10 fps | 640x480 下**人脸检出率 0%**, 所以不能降 |
| 判定循环 | **4.9 Hz**(配置 5 Hz) | Pose 跑 640 缩略图, Face 跑头部裁剪图 |
| CPU | 约 12% 单核 | 跑一整天没有负担 |
| Pose 检出率 | 100% | 人在画面里 |
| 人脸检出率 | **99–100%**(头框中位 280px) | 看手机能否工作的前提 |
| 模型加载 | ~1 秒 | 很快 |
| **模型 close()** | **42 秒** | 已用 daemon 线程绕开, 否则关程序像卡死 |
| 磁盘/内存 | 模型 9 MB, 依赖约 250 MB | 首次下载模型后完全离线 |

### 你的校准结果(`calibration.json`)

| 姿势 | yaw | pitch |
|---|---|---|
| 看屏幕 | 1.7° ± 0.4 | 6.5° ± 0.7 |
| 低头看书 | −0.4° ± 0.6 | 28.3° ± 0.8 |
| 转头看手机 | 36.3° ± 10.5 | 12.4° ± 2.3 |

阈值:看手机 `yaw > 19.0°`,低头 `pitch > 17.4°`;符号自学习得出 `yaw_sign = −1`
(MediaPipe 原始 yaw 方向是反的 —— 硬编码正负会让"看手机"信号完全失效)。

## 三、两种跑法

**试跑(带终端输出,能看见它凭什么提醒你):**

```powershell
cd 'E:\Code\attention_please'
.\.venv\Scripts\python.exe scripts\run.py
```

**日常(只有托盘图标,无控制台):**

```powershell
.\.venv\Scripts\pythonw.exe scripts\run_tray.py
```

**开机自启**(在"启动"文件夹放快捷方式,随时可撤销):

```powershell
.\.venv\Scripts\python.exe scripts\autostart.py install
.\.venv\Scripts\python.exe scripts\autostart.py uninstall
```

- 同一个时间只能跑一个实例(端口 47731 当互斥锁),第二个会被拦住并提示;
- 程序每分钟热重载 `config.toml`,改完保存即可生效,不用重启;
- 每 30 秒落盘一次(这台机器 4 天蓝屏两次,不能把 5 分钟数据押在内存里)。

## 四、试跑期间请观察三件事

1. **误报次数**(最重要):有没有在你**认真学习**的时候响/弹窗?目标 ≤2 次/小时。
   觉得烦就记下来 —— 那是要调的信号,不是你的问题。
2. **安静时段是否合适**:18:00 后只弹窗不发声。要改就改 `config.toml` 的
   `[quiet_hours] ranges`(例如改成 `[]` 全天出声,或 `["22:00-24:00"]`)。
3. **人脸覆盖率**:试跑终端每分钟一行 `判定中·数学/政治 | 4.9 Hz | 人脸 99%`。
   掉到 80% 以下说明你的坐姿/距离让"看手机"不可靠,先跑
   `scripts\tune_distance.py` 调姿势。

## 五、已知问题与限制(诚实清单)

1. ~~**眨眼基线严重偏低**~~ → **已随发呆一起作废**(2026-09-18):校准测出 3.0 次/分,
   正常人 15–20 次/分,说明眨眼检测严重欠计数 —— 这个判据本身就不可用。
   现在**没有任何信号消费眨眼数据**(发呆已砍, 见七之十三), 采集层仍在测但只作记录。
2. **摄像头 720p 只有 ~10 fps**:USB2 未压缩带宽所限。判定 5 Hz 够用,别指望更高。
3. **本机有硬件隐患**:`0x124 (WHEA_UNCORRECTABLE_ERROR)`,4 天蓝屏两次(9/13、9/17),
   与本程序无关(那时还没有这个项目)。仍建议查散热/BIOS/驱动。
4. **本机有 OBS 虚拟摄像头**(索引 2,静帧占位图)和红外摄像头(索引 1,灰度)。
   真摄像头是**索引 0**,选设备必须人眼确认(`scripts/pick_camera.py`)。
5. **pip 必须加 `--no-cache-dir`**:默认缓存目录在工作区之外,受限环境下会让 pip 挂死。
6. **PowerShell 的 TLS 在这台机器上是坏的**(schannel 拿不到凭据),下载一律用 Python。
7. **托盘版已真人验证**(见"验收记录"),但**目前这个实例挂在我的会话下**;
   要让它独立于聊天,自己在终端跑 `pythonw scripts\run_tray.py`,或装开机自启。
8. **人脸覆盖率与"看手机"天然冲突**:你测试时转头看手机那一分钟,人脸覆盖率掉到 24%
   (因为头转得太偏,人脸模型检不到)。这意味着**"看手机"可能在最该抓的时候抓不到**
   (漏报,不是误报)。**计划**:M4 加一条兜底——人脸丢失但 Pose 显示头部明显右偏时,
   记为"看不清/疑似看手机",只记录不报警。
9. **漂移风险**:你的坐姿/笔记本高度一变,校准阈值就不准了。建议每次大改桌面布置后
   重跑一次 `scripts\calibrate.py`(1 分钟),或先跑 `scripts\tune_distance.py` 看覆盖率。

## 六、信号启用状态(2026-09-18 傍晚更新)

| 信号 | 状态 | 说明 |
|---|---|---|
| 切到娱乐窗口(screen) | ✅ **已启用** | 窗口标题 + "能看到脸且没低头" |
| 看手机(phone) | ✅ **已启用**(2026-09-18 12:48 热重载生效) | 头右偏 > 19° 连续 **15 秒**(9/18 从 25 调小); 升级上限"中"(最多三声)。**9/18 全天 6 段 / 2.8 分钟, yaw 22°–55°, 未见误报** |
| 离开太久(away) | ✅ **已启用** | 离开 > 15 分钟提醒一次。代码在跑(13:30 重启过), 但当天唯一一次离开只有 10.8 分钟, **还没够到门槛, 所以仍未真机响过** |
| **Pose 弱证据(pose)** | ⏳ **代码已就绪, 实例是旧代码** | **只记录不报警**(上限硬 0 + 强制 record_only)。用途: 把日报的「看不清」拆成"疑似纸笔模式" vs "真的看不到"。重启托盘后生效 |
| ~~发呆(daze)~~ | ❌ **已于 2026-09-18 砍掉** | 三条证据: ①依赖人脸关键点而低头时覆盖率只有 12-15%; ②眨眼闸门几乎永不成立(既不误报也不生效); ③改用 Pose 报警会把"认真写题"判成发呆。详见七之十三 |

**看手机为什么可以开了**:模拟用你的真实校准值跑过 ——
正常看屏幕(yaw 1.7°)、低头写题(yaw −0.4°)、右偏 25° 只拿 20 秒东西, **都不提醒**;
转头看手机(yaw 36.3°)**15 秒后 L1、25 秒后 L2**(门槛 9/18 13:08 从 25 秒调小为 15 秒);
背单词时段(`allow_phone = true`)只记录不报警。

**调小门槛的代价(必须知道)**:同一个模拟里, "向右偏 25° 拿东西 **20 秒**"以前不提醒,
现在**会提醒一声** —— 因为"回一条消息"通常 20-40 秒, 要抓住它就必然更容易被"向右伸手"触发。
如果发现误报, 把 `phone_continuous_seconds` 往上调回 20-25 即可(配置热重载, 一分钟生效)。
加上两天试跑 0 次"判定错了"、人脸覆盖率 93%(这个信号依赖人脸),条件已经满足。

## 七、下一步(等你发话)

1. ~~决定是否安装开机自启~~ → **已安装**(2026-09-17):
   `C:\Users\Wu Fan\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\attention_please.lnk`
   → `pythonw.exe scripts\run_tray.py`,**下次登录自动启动**。
   不想用了就 `python scripts\autostart.py uninstall`,或者直接把那个快捷方式删掉。
2. ~~试跑满意后打开 phone 信号~~ → **已于 2026-09-18 12:48 开启**(热重载,无需重启)。
   接下来观察:看手机有没有误报(yaw 证据会写在弹窗里);太敏感就把
   `phone_continuous_seconds` 从 15 往上调回 20-25。
3. 攒够误报日志后,用 `data/events.sqlite3` 里的 `wrong` 事件人工调阈值。
4. ~~**M4 的关键改动(今晚才看清)**~~ → **已实现**(见七之十二/七之十三):"低头写题"时
   **人脸基本看不到**(实测该时段覆盖率仅 37%),而"发呆"信号恰恰要求低头姿态 + 眨眼 ——
   它依赖人脸关键点, 所以在最需要它的场景下**天然不可用**。
   兜底方案就是**在人脸丢失时改用 Pose 的头部关键点做低精度头姿**, 让"低头/右偏/长时间不动"
   在看不到脸时仍能工作(Pose 检出率实测 100%)。代价是精度更低 —— 所以它**只记录不报警**。
   而**发呆本身已于 2026-09-18 砍掉**(三条证据见七之十三)。

## 七之二、验收记录(2026-09-17 晚)

托盘版逐项真人验证通过:图标颜色(绿/黄)、悬停提示、暂停(理由必填,不写不给暂停)、
恢复、手动学习通知、截图文件夹提示、**自带日报查看器**、退出。

验证过程中发现并修掉 4 个真 bug(都已加回归测试):

1. **SQLite 跨线程**:`Runtime` 在主线程建连接、监控循环在工作线程用 —— 每一帧都抛
   `ProgrammingError`,整条监控链路静默瘫痪,而托盘菜单还是好的,表现就是
   "图标在、菜单能点、但怎么点都没反应"。改成每线程一份连接 + WAL + 退出时统一关闭。
2. **托盘图标刷新循环退出条件写错**:线程在 `icon.run()` 之前启动,那一刻
   `icon.visible` 还是 False,循环直接退出 → 暂停后图标永远不变黄。
3. **`.md` 没有文件关联**:`os.startfile` 静默失败。而且 VS Code 装在 **D 盘**,
   不能写死 C 盘路径。
4. **后台进程启动的 GUI 抢不到前台**:VS Code 确实打开了日报,但窗口开在别的窗口后面
   —— 看起来就像"没反应"。所以日报改成**自带 Tk 查看器**(一定能看见),
   VS Code 降级为窗口里的次要按钮。
5. 另外把"没有可见效果"的菜单动作(手动学习、清空截图、查看截图)都加上了
   Windows 通知,不然用户没法判断按钮到底有没有生效。

## 七之三、事故记录(2026-09-17 21:00): 该提醒却没提醒, 而且什么都查不到

**现象**: 20:55-21:10 你在 QQ / 微信 上待了 18 分钟, 数据库里有 9 段分心记录,
但**只有 20:54 那两条提醒**。用真实时间戳回放状态机, 本该额外发出 **3 次 L3**
(20:58:58 / 21:02:28 / 21:05:45)。

**根因(2026-09-17 21:46 定位并修复)**:`signals.py` 的 `update()` 里有这么一句——

```python
self._update_away(obs, actions)
if obs.ts < self.resume_until:      # "刚坐下 20 秒内别念你"的宽限期
    return actions                  # ← 却顺手把整个信号判定跳过了
```

而 `_update_away()` 会在**每一个"检测到人"的 tick** 把 `resume_until` 推成 `now + 20 秒`。
于是只要 **Pose 在通/断之间抖动**(低头、转头、光线差时极常见),每个"通"的 tick 都会把
宽限期往后推 → **`_update_signal` 几乎永远不执行**。它同时解释了当年所有怪现象:

| 现象 | 解释 |
|---|---|
| 只响一次 L1, 后续 209 秒一声不吭 | 升级逻辑在"被跳过的判定"里 |
| 分集长达 209s / 200s / 187s | 断的间隙都没超过 5 秒迟滞, 而结束判定也被跳过 |
| 离开后不报告"回到座位" | 同一段代码被跳过(那晚还叠加了一次重启, 状态被重置) |
| 面对面聊天/学习时完全没事 | 那时 Pose 稳定, 宽限期根本没被推 |

**旁证**:那晚的每分钟人脸覆盖率在 **0% ↔ 96%** 之间来回跳 —— 正是抖动。

**修法**:记账永远照跑, 宽限期只用于"别刚坐下就念你"(抑制提醒, 不抑制记账):

```python
# _update_away: 只有"真的报告过离开、然后真的回来了"才给宽限期
if returned:
    self.resume_until = max(self.resume_until, obs.ts + det.resume_grace_seconds)

# _update_signal: 宽限期只让提醒闭嘴, 不影响分集的开始/结束/升级
muted = (obs.ts < tr.muted_until
         or obs.ts < self.resume_until
         or (tr.last_episode_end is not None
             and obs.ts - tr.last_episode_end < rem.cooldown_seconds))
```

**回归测试**:`tests/test_pose_flicker.py`(8 项)——
每两帧抖一次仍必须升到 L2/L3、单帧抖动不许推迟升级、抖动不许报"离开"、
真离开 90 秒后回来必须报告、宽限期内既不许吵也不许漏记账。

**已排除**:

| 怀疑 | 证据 | 结论 |
|---|---|---|
| 循环没跑 | 那 18 分钟每分钟 299–300 tick | 否 |
| 摄像头故障 | 无 camera_busy / read_fail 记录 | 否 |
| 配置被改 | config.toml mtime 仍是 20:19 | 否 |
| 冷却吞掉了 | 回放显示 20:58:58 冷却已过期, 仍发出 L3 | 否 |
| 状态机升级逻辑错 | 单测 + 真实时间戳回放都正常 | 否 |
| **坐下宽限期被抖动反复重置** | **代码审查 + 抖动回归测试复现** | **是** |

**教训**:宽限期/静默这类"抑制"只能抑制提醒, **绝不能抑制记账** ——
否则一旦它被某个高频事件反复重置, 整个判定链就会静默饿死, 而且没有任何痕迹。

**加固(已实现 + 有回归测试)**:

1. **文件日志** `data/logs/YYYY-MM-DD.log`:启动时把 stdout/stderr 接到日志上,
   **一行不改**就留下全部输出(连 MediaPipe 的警告一起), 每条即时 flush
   (这台机器有蓝屏前科), 默认留 14 天;
2. **先记账, 再提醒**:提醒通道(声音/弹窗)整段包在 try 里 —— 原来顺序是反的,
   记录写在提醒之后, 提醒一抛异常, 连"它本来想提醒"这件事都留不下来;
3. **派发留痕**:每次派发前把"状态机决定了什么"写进日志, 从而能一眼区分
   *状态机没发出* / *派发了但记账失败* / *提醒通道报错*;
4. **每分钟状态行**增加状态机快照, 例如
   `判定中·数学/政治 | 5.0 Hz | 人脸 88% | screen:分集中 120s/L3 静默剩 60s`;
5. 所有异常带完整 traceback 进日志, 而不是只打印一行。

**下次重启托盘版即生效。如果再次出现"该提醒却没提醒", `data/logs/` 里会有答案。**

**文件日志当晚就回本了**:21:23:17 你点开日报时抛的 `TclError` 被完整记了下来
(见"七之四"), 而它以前只会被打进虚空。

## 七之四、事故记录(UI 永久失效): **已定位并修复**

**现象(你报的)**: 打开一次"今日日报"并关闭后, 日报再也打不开, 连"暂停监控"的弹窗也弹不出来。

**根因(已用真实窗口的回归测试复现)**:

```python
bar = tk.Frame(win, bg="#1b1b1b", padx=12, pady=(0, 10))
#                                          ^^^^^^^^^^ 这里错了
# → TclError: bad screen distance "0 10"
```

`tk.Frame` 的 `padx/pady` 是**控件选项, 只接受整数**;`(0, 10)` 这种元组只有
`pack()` / `grid()` 才认。而这个异常发生在**日报窗口已经画出来之后**(构造底部按钮栏时)
才抛出, 于是:

1. 你**看到了**日报窗口 → 以为功能正常;
2. 但异常让 `tick()` 中断, 而 `root.after(200, tick)` 恰好写在最后一行 →
   **整个 UI 轮询链永久断掉**:mainloop 还活着, 但再也没人处理请求;
3. 所以之后再打开日报、以及暂停弹窗, **全部静默失效**。

**两个让它变致命的放大器**(也都已修):

- `root.after()` 不在 `finally` 里 → 任何一次异常都能永久杀死 UI;
- 用户点右上角 X 关窗时 Tk 直接销毁 widget, 而 `state["win"]` 还指着那个死 widget →
  下一轮算倒计时抛 `TclError`(同样杀死 UI)。现在用 `<Destroy>` 事件复位状态。

**加固**:

1. 修掉元组 `pady`(挪进 `.pack(pady=(0, 10))`);
2. `root.after()` 移入 `finally` —— UI 再也不会被一次异常永久杀死;
3. `<Destroy>` 绑定复位状态, 覆盖"点 X 关闭"这条路径;
4. 接管 `root.report_callback_exception`, Tk 回调异常不再只打进虚空;
5. 新增 `ui.windows_shown` / `ui.last_ui_error` 两个诊断字段, 并发出 `ui_error` 事件
   (会被写进文件日志);
6. **新增两类测试**:
   - `tests/test_ui_static.py`(常规套件, 1 秒):静态扫描"元组写进控件构造函数"这类错;
   - `tests/test_ui_lifecycle.py`(**真开窗口**, 7 项, 用 `AP_UI_TESTS=1` 手动跑):
     开→X 关→再开、日报关掉后暂停框还能弹、提醒窗 X 关后还能再弹…… 全部通过。

**教训(对我自己)**: 上次"验收通过"是被骗的 —— 窗口先出现、后失败, 看起来就像成功。
所以现在关键路径都改成了**可断言的行为**(窗口计数、错误字段), 而不是"你看到没有"。

## 七之五、第二个漏洞: 冷却锚点错了(2026-09-17 21:52 修复)

**发现方式**:看日志时发现 21:49:04 起的一段分心 **109 秒完全静默**
(快照里 `screen:分集中65s/L0` —— 分了 65 秒, 等级还是 0)。

**原因**:冷却(`cooldown_seconds = 180`)的锚点挂在"**上一次分集结束**"上。于是

```
切窗口 -> 分集结束 -> 马上又切过去 -> 分集结束 -> ...
```

会把冷却**无限顺延** —— 这正是"连续 18 分钟一声不吭"的另一半原因。
而且 21:49:04 那一段从头到尾都落在静音窗口里(锚点 21:48:52 + 180s = 21:51:52),
它 21:50:44 就结束了, 所以**整整 109 秒没有任何提醒**。

**修法(两条)**:

1. **锚点改成"上一次真的响过的时刻"**, 而且冷却只约束**这一集的第一声**
   —— 已经开始提醒的这一集, 升级永远不受冷却影响
   (否则 L2/L3 会被自己的 L1 挡住, 永远发不出来);
2. **新增静默上限** `max_silent_episode_seconds = 90`:
   一次分心**连续**超过 90 秒还没出过声, 就强行提醒一次(每集只强推一次)。
   "你明确说判定错了"(wrong) 是唯一能压过它的东西。

**验证(用今天真实分集时间线回放)**:

| 分集 | 时长 | 原提醒数 | 修复后 |
|---|---|---|---|
| 20:56:09 | 199s | 0 | 1 |
| 20:59:48 | 177s | 0 | 1 |
| 21:03:14 | 190s | 0 | 2 |
| 21:06:45 | 81s | 0 | 3 |
| 21:09:37 | 54s | 0 | 1 |
| 21:43:06 | 46s | 1 | 3 |
| 21:49:04 | 100s | 0 | 1 |
| **合计** | | **7** | **18** |

**6 段分心从"完全没人管"变成"会被提醒"。**

**回归测试**:`tests/test_cooldown.py`(5 项)——
短促的连环切换仍保持安静(冷却的本意不能被破坏)、超过静默上限必须出声、
**升级不被冷却挡住**、每集只强推一次、`判定错了` 能压过一切。

## 七之六、第三个漏洞: "低头写题被误判成在看娱乐窗口"(2026-09-17 22:0x 修复)

**你怎么发现的**:你说"我刚刚应该一直低头在写题目" —— 而系统当时正在提醒"切到了娱乐窗口"。

**根因**(`signals.py` 的 `evaluate()`):

```python
looking_at_screen = obs.pitch is None or obs.pitch < cal.book_pitch_deg
#                    ^^^^^^^^^^^^^^^^^^^^ 拿不到头姿 = 看不到你的脸 ...
#                                         却被当成了"你在看屏幕"
```

低头写题时脸会转出画面 → `pitch is None` → 旧逻辑判"你在看屏幕" → 于是
**"把 QQ / B 站晾在前台、自己低头写题"一路被当成分心**。这正是你说过最烦的误报。

**修法**(回到你最初的设计:**低头 = 纸笔模式, 不判窗口**):

```python
face_visible = obs.pitch is not None             # 看不到脸 -> 不知道你在看什么, 不猜
head_up      = face_visible and obs.pitch < 阈值  # 头没低下 -> 确实在看屏幕
screen_active = pose_present and head_up and 标题命中黑名单
```

代价是"靠回椅背、脸出画面地看视频"会漏判 —— 但按"误报最烦"的原则这是对的,
而且这段时间会被覆盖率如实记成**看不清**写进日报, 不会假装你在专注。

**对今晚数据的影响**(分集时间 × 每分钟人脸覆盖率交叉):

| 分集 | 人脸覆盖率 | 结论 |
|---|---|---|
| 21:48:35 QQ | **100%** | 基本可信(你当时确实在看 QQ) |
| 21:33:51 QQ | 48% | 存疑 |
| 21:49:04 B 站(100 秒) | **37%** | **很可能就是"低头写题"被误判** |
| 其余 10 段 | 57–97% | 基本可信 |

**所以今晚日报里的"分心 14.5 分钟 / 有效专注 0.48 小时"是失真的** ——
那 100 秒(以及可能的几分钟)其实你在写题。这个洞不只造成误报, 还污染了指标。

**回归测试**:`tests/test_screen_evidence.py`(9 项)——
低头写题 + 前台娱乐窗口不判、看不到脸绝不判、刚碰过键鼠也不能替我下结论、
头没低看娱乐窗口要判、白名单仍然优先、以及一条端到端的"低头写题 8 分钟必须全程静默"。

## 七之七、2026-09-18 上午实测: **没有 bug**, 而且三个修复都生效了

| 检查项 | 结果 |
|---|---|
| 异常/报错 | **0 条**(日志里那 17 条 ERR 全是 9/17 修 UI 之前的) |
| 提醒链路 | 09:11:44 L1 → 09:11:54 L2,证据写着「QQ(**你在看屏幕**)」 |
| **误报(上次的洞)** | 11:27–11:28 覆盖率掉到 **12–15%**(低头刷题)→ **一声没响** ✅ |
| 冷却 | 09:12:38 那段 25 秒的连环 QQ 被静音(刚提醒过 30 秒)→ 符合"别连环叫" ✅ |
| 暂停理由 | 09:44–10:02 暂停 17 分钟,理由「鸡蛋煮熟了,吃早饭」进了日报 ✅ |
| 稳定性 | 34017 tick、5.0 Hz、人脸覆盖率 **93%**(前一天 69%) |
| 计划执行 | 只在 focus 块内判定:10:30–10:40 休息、11:30 后午休都自动待机 ✅ |

> 附注:日志在 11:32 之后没有新行**不是故障** —— 11:30 之后是午休,程序正确地处于待机。

## 七之八、报表口径有误导(2026-09-18 中午修复)

**现象**:上午的日报写"有效专注 1 小时 48 分 / 计划 9 小时 30 分 = **19%**",
看起来像摸了一天鱼 —— 但程序是 09:09 才启动的,08:30–09:09 根本没有数据。

**根因**:比值分母用了"**计划时长**",于是"机器没开机 / 程序没跑 / 暂停"
全被算成了"你没专注"。这正是"漏报必须可见"没做到位。

**修法**:

1. 比值分母改成**实际监控时长**(真正在判定的时间),计划时长只作上下文 ——
   今天因此从"19%"变成"**96%**";
2. 新增两条警告:
   - "计划里的「高数强化听课」从 08:30 开始, 但监控是 09:09 才起的 ——
     那段时间没有数据(机器没开机/程序没跑), 不要当成没专注";
   - "计划里**已经过去**的 2 小时 50 分中, 有约 57 分钟没有监控数据"。
     (分母必须是"已过去"的计划时段: 中午生成日报时下午还没到, 用全天计划会算出
     "有 7 小时没监控"这种吓人但没意义的数字。)

**回归测试**:`tests/test_report_coverage.py`(6 项) —— 监控时长取自 tick 数、
比值按监控时间算、晚开机必须警告、准时开机不许乱警告、空数据要明说、
有效专注 = 监控 − 分心 − 看不清 − 离开。

## 七之九、"人不在检测框里"的行为, 以及因此发现的两个漏洞(2026-09-18 修复)

**现在的行为**(照着代码):

| 时间线 | 行为 |
|---|---|
| 刚消失 | 只记一个时间戳, 什么也不做 |
| 0–90 秒 | 静默; 但正在进行的**分心分集会在 5 秒迟滞后结束并入库**, 新分集无法开始 |
| 满 90 秒(`away_seconds`, 可配) | 记 `away_start`, 终端打印「💤 离开座位(只记录, 不提醒)」——**不响、不弹窗** |
| 期间 | 摄像头**继续开着**(判定中就不释放); 覆盖率 <50% 的分钟记为「看不清」, 既不算专注也不算分心 |
| 回来 | 记 `away_end`(含时长), 并给 **20 秒坐下宽限期**(只记录不提醒) |
| 日报 | 「离开座位」= 当天 away_end 之和, 从有效专注里扣除 |

**漏洞 1: 离开时长会跨过"不判定"的时段。**
`suspend()` 以前不碰离开状态, 于是这条时间线会算错:

```
11:20 离开(已报告) -> 11:30 时间表结束(不判定) -> 午休 -> 13:00 回来
```

回来时算出"离开 1 小时 40 分", 其中 1.5 小时是午休 —— 而午休**本来就不在监控时间里**,
日报却把这段从未计入的时间又从"有效专注"里扣掉一次(可能直接扣成 0)。
**修法**: 判定一停就把离开按当前时刻结掉; 恢复判定后若人还不在, 90 秒后重新开一段。

**漏洞 2: 人不在画面里时, 托盘图标还是绿的。**
你会以为它在管你, 其实它谁也看不见。**修法**: 新增第 5 种状态 **蓝 = 离开座位(只记录)**,
悬停提示也写明。

**回归测试**: `tests/test_away.py`(10 项)—— 90 秒门槛、短暂离开不留痕、回来报时长、
分心到一半走人要在 5 秒内结束分集、判定停止时收口离开、午休不计入离开时长、
午休后仍在外面要重新开一段、托盘必须有独立的"离开"颜色与状态。

## 七之十、"离开太久"提醒(2026-09-18 按你的要求新增)

**背景**:原来"离开座位"完全只记录不提醒 —— 但如果你在"高数强化听课"时段跑去床上躺两小时,
程序一声不吭。对拖延症来说这是个明显的口子。

**现在的行为**:

- **离开超过 15 分钟**(`[detection] away_reminder_seconds = 900`,**0 = 关闭**)提醒**一次**;
- 级别上限是"低"(1 声), 所以**一次离开只提醒一次**, 不会连环叫;
- **通道跟着安静时段走**:白天响一声, 18:00 后自动变成只弹窗;
- **"离开"的弹窗不会自动关闭** —— 你不在座位上, 响的时候听不到, 回来得能看见它凭什么响过;
- 它**不算"分心"**:不计入分集次数、不计入分心时长(那段时间已经在"离开座位"里了),
  只在日报里多一句"离开座位 30 分钟(提醒过 1 次)"。

**实现**:复用了现成的分级机制(新增 `SignalKind.AWAY`, 可信度 low), 所以冷却、
"判定错了"静默 15 分钟、安静时段降级全部自动生效。

**顺手抓到的两个 bug**:

1. **`snapshot()` 的键名冲突**(我自己刚引入的):布尔量 `"away"` 被同名信号的字典覆盖,
   字典恒为真 → **判定中时托盘永远显示蓝色**。`test_away.py` 的托盘测试当场抓住。
2. 离开的分集如果按普通分集统计, 会让有效专注被**扣两次**(离开时长 + 分心时长)。

**回归测试**:`tests/test_away.py` 增至 20 项 —— 10 分钟不提醒、16 分钟提醒一次、
离开一小时也只提醒一次、`=0` 时关闭、白天发声/安静时段不发声、
回来后再离开能重新提醒、离开不算分心、离开提醒必弹窗且不自动关闭、
普通 L1 有声时仍然不弹窗。

## 七之十一、GBK 控制台吞掉提醒(2026-09-18 定位并修复)

**现象**:单测偶尔报 `FAILED (errors=12)`, 而"同一条命令加不加
`PYTHONIOENCODING=utf-8` 结果不同" —— 一度被当成"偶发 flake"。

**根因**:

```
UnicodeEncodeError: 'gbk' codec can't encode character '\U0001f514' (🔔)
  File "runtime.py", line 438, in dispatch
```

中文 Windows 控制台默认 **GBK**, 而提醒文案里有 emoji。`dispatch` 的顺序是
`记账 → print → 响铃 → 弹窗`, print 一抛, **响铃和弹窗被整段跳过** ——
**记账了但没提醒**, 正好违反"记账优先于提醒"。

**修法**:`logbook.safe_print()`(降级不抛) + runtime 全部走 `self._say()`;
新增 `tests/test_encoding.py`(7 项)。**UTF-8 / GBK / 不设编码 三种情况各 214 项全绿。**

**顺带**:项目已从 OneDrive 目录树搬到 **`E:\Code\attention_please`**
(自启快捷方式已重装指向新路径), 顺带消除了"带人脸的分心截图躺在云同步目录里"这个隐患。

## 七之十二、M4 第一步: Pose 弱证据通道(2026-09-18 下午)

**要解决的问题**:低头写题时人脸覆盖率只有 12-15%, 而"发呆""看手机"都依赖人脸关键点 ——
在**最需要它们的场景下天然不可用**。这个洞有多大, 当天数据说话:

| 指标(9/18 09:09–18:14) | 数值 |
|---|---|
| 实际监控 | 6 小时 15 分 |
| **「看不清」(脸不在画面)** | **63.9 分钟 = 监控时长的 17%** |
| 17:33–17:51 的人脸覆盖率 | 长期在 **0–10%** |
| 分心 | 10.2 分钟(19 段) |
| 有效专注 | 4 小时 50 分 |

也就是说**日报有六分之一的时间是瞎的**。M4 要修的就是这个。

### 调研结论(离线跑 `captures/*.jpg` 的摄像头那一半, 脚本 `probe_out/posenoise.py`)

| 结论 | 证据 |
|---|---|
| **Pose 兜底通道确实存在** | 人脸覆盖率 <60% 的 54 个分钟里, Pose 仍有 **80.6%** 的帧在; 21:49(疑似低头写题)人脸 33%, Pose **100%** |
| **Pose 关键点 0-10 的 `visibility` 恒为 1.0**, 哪怕头转了 41° | 所以**不能拿它当质量闸门**(与直觉相反) —— 必须用几何 |
| 质量闸门必须用几何 | 抓到一帧肩宽 **96px vs 常态 204px**(部分出画), 那一帧所有比值都是垃圾 |
| `side`(鼻相对肩中线/肩宽)可判「转头」 | yaw≈0 时 \|side\|<0.15; yaw −34°/−41° 时 −0.45/−0.62 |
| **`bow` 只能相对判断** | 与真人脸 pitch 只有 **r = −0.66**; 直立帧的 bow 本身就在 0.35–0.78 铺开 |

**⚠️ 最关键的一条(它推翻了一版设计)**:第一版判据是"中位数 ± k·MAD"。
用**真实数字**算出来的死区是 **0.39**, **比低头造成的位移(0.35)还大** →
真低头一帧都判不出来。所以改成**分位数范围**(直立基准的 p10/p90):
低头只要"掉出你平时直立的范围"就算。
`tests/test_pose_head.py::test_median_band_would_have_failed` 是**反向测试**, 钉住这个决策。

**教训**:这类"被相机几何污染的量"**不能定绝对阈值** —— 相机高度、坐姿、前后距离都会改它。
只能拿"你自己最近的样子"当基准。这条同时适用于眨眼(校准测出 3.0/分, 正常人 15-20)。

### 做出来的东西

- `src/attention_please/baseline.py` —— 滚动基线(中位数/MAD/p10/p90 + 可信样本闸门 +
  样本不够就弃权 + 抽稀 + 带下限的死区);
- `src/attention_please/pose_head.py` —— Pose 粗头姿: 尺度闸门 + 短窗中位数 +
  **直立基准只吃"人脸确认头没低"的帧**(所以长时间低头不会污染基准)+ 拿不准就弃权;
- 接线:`SignalKind.POSE`(**只记录不报警**: 上限硬 0 + 结构上强制 `record_only`, 两道闸门),
  日报把「看不清」拆成"疑似纸笔模式" vs "什么都没看到";
- **顺手抓到两个真 bug**:
  1. `Policy.enabled` 的默认值写死 `{screen,phone,daze}` —— 后果是**新信号在单测里永不触发、
     生产里却会触发**, 而单测正是用来兜底的。现在默认"全开"。
  2. `busiest_distraction_hours` 把 `away` 分集也算成了分心("最常分心的时段"被污染)。
- **单测 214 → 285 项全绿**(新增 71 项)。

### 还没做

- **真人标定** `pose_head_min_margin`:现在的默认 `0.08` **只由 1 个真人低头样本支撑**。
  摄像头空闲时跑 `probe_out/poselabel.py`(约 2 分钟, 屏幕上有姿势提示 + 倒计时),
  它会直接给出"分得开/分不开"的结论。
- **发呆的处置**:补充一个发现 —— **发呆目前其实是"哑"的**:它的眨眼闸门用校准基线 3.0/分,
  `blink_rate < 1.5` 几乎永不成立, 所以它既不误报也不生效。是修(自适应基线)还是砍
  (按预案只留 screen + phone), 等标定数据。
- ~~实例还是旧代码~~ → **18:56 已重启**,当前实例含 pose 通道 + 全部修复。

## 七之十三、M4 真人标定 + 第二个"基准污染"通道(2026-09-18 18:48)

### 标定怎么做的

`probe_out/poselabel.py`:5 段姿势(看屏幕 45s / 低头写题 25s / 转头看手机 25s /
侧身后靠 25s / 回到看屏幕 20s),共 3740 帧。

**第一次跑废掉了, 原因值得记**:没有提示音, 于是你在第 2 阶段开头还保持了 3 秒直立,
那几帧把"低头 p90"抬到 0.509, 脚本算出 `直立 p10 − 低头 p90 = -0.000`, 并打印
**"分不开, 该砍掉低头判定"** —— 而把那段延迟剔掉之后, 两者其实隔了 **0.138**。

> 教训:自动化结论必须能区分"数据说不行"和"数据被污染了"。**提示音不是体验问题, 是数据质量
> 问题**。现在的脚本:每段用提示音报序号(响 N 声 = 第 N 个姿势, 因为低头/转头时看不了屏幕),
> 且开头 2.5 秒的样本不计入统计。

### 标定结果

| 判据 | 原始数据 | 结论 |
|---|---|---|
| **低头** | 直立 bow p10 **0.491**(最小 0.459) vs 低头 bow p90 **0.343**(最大 0.376) | 分得开, 间隔 **0.149** |
| **转头** | 直立 side −0.071..+0.084 vs 转头 side **−0.666..−0.401** | 分得开, 间隔 **0.482** |
| 侧身/后靠 | 肩宽 205px vs 直立 329px(比值 0.62) | **100% 弃权** —— 尺度闸门正确拒绝 |

`pose_head_min_margin` 定稿 **0.08**(阈值落在 0.343 和 0.459 正中间)。

### 第二个"基准污染"通道(真 bug, 已修)

回放真实数据时发现一个刺眼的自相矛盾:转头原始数据隔了 0.482, **却只判出 14%**。

根因:直立基准的可信闸门**只检查了 pitch**。但"头没低"**不等于**"头朝前" ——
看手机的时候头是抬着的(pitch 正常), 于是整整 25 秒的转头帧都被当成直立样本喂进基准。
它们占据了窗口里最小的那些排名, 把基准的 side p10 从 −0.03 **拖到 −0.5**, 阈值被推到
−0.58, 只剩最偏的 14% 还能判出来。

修法:可信样本要求 `|face_yaw| < phone_yaw_deg`(本机 19°)。

**验证**(`probe_out/replay_poselabel.py`, 用真实逐帧数据回放):

| 阶段 | 修复前 | 修复后 |
|---|---|---|
| 看屏幕 误报 | 0% | **0.0%** |
| 低头写题 检出 | 93% | **98%** |
| 转头看手机 检出 | **14%** | **98%** |
| 侧身/后靠 | —— | 100% 弃权(正确) |
| 回到看屏幕 误报 | —— | 0.6%(3 帧, 全在换姿势后 2.5s 内、原始 side 是 −0.01 → 3 秒平滑窗的正常滞后, 不是误报) |

> 教训:相对判据的**锚**(基准)比判据本身更容易错, 而且错了以后的表现是"检测率莫名很低",
> 不像崩溃那么显眼。任何"用 X 判断样本可信"的闸门, 都要问一句:**X 成立就等于它可信吗?**

### 顺带:只记录的分集不许截图(真机验证抓到)

18:28 真机跑 pose 通道时, 一段 33 秒的 POSE 分集也存了一张"屏幕+摄像头"拼接图。
截图是给**分心现场证据**用的, 而 POSE 分集只是"看不到脸时 Pose 觉得你在低头" ——
给它存图既是隐私问题(低头写题会被拍一堆), 也会把 `captures/` 塞满。已修。

### 顺带观察(9/18 全天真实数据)

- `phone` 信号:**6 段 / 2.8 分钟**, yaw 证据 **22°–55°**(阈值 19°)→ **未见误报**;
- `wrong`(「判定错了」)**0 次**;`camera_busy` **0 次**;
- 唯一一次离开座位 **10.8 分钟**(15:11:30–15:20:49), **没够到 15 分钟门槛** →
  「离座 15 分钟提醒」到现在还没在真机上响过(代码在跑, 缺的只是一次更长的离开)。

### 发呆的处置:**已砍掉**(2026-09-18 用户拍板)

三条独立证据(现在写进代码注释与文档, 复活前必须先推翻它们):
1. 它依赖人脸关键点, 而低头写题时人脸覆盖率只有 12–15% —— **在最需要它的场景下天然不可用**;
2. 它的眨眼闸门用校准基线 3.0/分(正常人 15-20), `blink_rate < 1.5` 几乎永不成立 →
   **它现在既不误报也不生效**;
3. **改用 Pose 来报警也不行**:Pose 判"低头"确实准(98%), 但"低头 + 头不动 4 分钟"正是
   **认真写题**的样子 —— 拿它报警就是最大的误报源。所以 Pose 通道**必须**保持"只记录"。

结论:Pose 通道已经拿到了 M4 想要的价值(把日报的「看不清」拆开, 9/18 全天有 63.9 分钟
「看不清」= 监控时长的 17%),而发呆作为**报警**信号没有可行的证据链 —— **已砍掉**。

**实际删掉的东西**:`SignalKind.DAZE`、`CONFIDENCE`/`SIGNAL_LABEL` 里的条目、`evaluate()`
里的发呆判定块、`Detection` 的 4 个 `daze_*` 键、config.toml 的对应注释与键、4 项发呆测试。
**保留的东西**:`Observation` 的 `yaw_std`/`pitch_std`/`blink_rate`/`eye_closed_ratio` ——
采集层仍在测, 留着当将来重做"头没动/眨眼"类判据的入口, 但**已无信号消费**(代码里已注释)。
**向后兼容**:老 config.toml 里残留的 `daze_*` 键会被忽略而不是报错 —— 有回归测试守着
(`test_config.py::test_leftover_daze_keys_still_load`)。

## 七之十四、别的程序要用摄像头时不让出(2026-09-18 用户报的 bug)

**现象**(用户报的):"当别的需要摄像头的软件打开时, 这个程序无法将摄像头权限让出来。"

**先查数据**: 当天 `camera_read_fail` / `camera_busy` 事件 **0 条**, 日志里也没有"读不到帧"。
也就是说**它压根没察觉到有人在等** —— 代码里唯一的让出路径(`cap.read()` 失败)从未触发。

**再复现机制**(一个进程开两个句柄):

| 第二个句柄的后端 | 能开吗 | 第一个还读得到吗 |
|---|---|---|
| `CAP_DSHOW` | 能开、能读 | **照样能读**(设备共享) |
| `CAP_ANY`(MSMF) | 能开, 读失败 | **全部失败**(MSMF 会把 DSHOW 的流踢掉) |

结论: **Windows 没有"别的程序想要摄像头"的通知接口**, 而我们的读一直成功, 所以只能主动让。

**修法(三层)**:
1. **自动让出**:前台窗口命中 `[camera_yield].apps` 关键词(腾讯会议/钉钉/飞书/Zoom/Teams/
   OBS/相机/视频通话…)就**立刻释放摄像头 + 暂停判定**, 托盘变**紫**, 日志写 `📷 已让出摄像头`;
2. **手动兜底**:托盘点"让出摄像头(30 分钟)" / "收回摄像头" —— 给关键词匹配不到的软件;
3. **不再拉锯**:摄像头打不开时的重试从固定 30 秒改成 30→60→120→240→300 秒封顶,
   免得每 30 秒把摄像头抢回来一次。

**两个容易踩的设计坑(都已写进注释与测试)**:
1. **关键词不能只是"程序开着"**, 必须特定到"正在用摄像头"。尤其**不许把"微信"/"QQ"放进去** ——
   它们在黑名单里, 前台一挂微信就让出摄像头 = 给自己开免监控后门, 而且正好在它该抓你的时候失效。
   要匹配视频通话用"视频通话"这类通话窗口才有的词。
   (实测自检: 微信 -> 不命中; "xxx 与 yyy 的视频通话" -> 命中。)
2. **时间表外不让出、也不记让出**: 那时本来就待机、摄像头已释放, 没什么可让的。
   不区分的话, 你晚上挂着会议室, 日报会把整个晚上算成"主动让出摄像头" —— 那是骗人的。

**可见性**(铁律 2): 让出期间**完全没有监控数据**, 所以日报会写一行
"📷 主动把摄像头让给别的程序共 N 分钟 —— 这段时间没有监控数据, 不是你没专注",
而不是让人以为"我明明在学却没记上"。

**回归测试**: `tests/test_camera_yield.py`(17 项)—— 命中就让 / 不命中不让 /
正常学习窗口绝不让(免监控后门)/ 暂停优先于让出 / 手动让出与到期 / 关闭开关 /
让出**立刻释放**而普通待机仍等 120 秒 / 关键词表里不许有"微信"/"QQ" / 记账与日报文案 /
释放日志不许说假原因。

### 真机验证(2026-09-18 20:17, 用 OBS)

用户打开 OBS, 一次成功。日志与事件库对得上:

```
20:17:33  📷 已让出摄像头(前台是「OBS」) —— 摄像头已让出, 判定暂停
20:17:34  (长时间不在判定区间, 已释放摄像头)      <-- 这条消息在骗人(见下)
20:17:38  📷 收回摄像头(让出 0.1 分钟)
20:17:40  [20:17:40] 判定中·数学/政治 | 4.5 Hz | 人脸 90%
```

事件库: `camera_yield`(20:17:33) → `camera_yield_end`(20:17:38, 4.29 秒)。
**收回后 2 秒就恢复判定**(摄像头重新打开 + 模型就绪), 没有卡住。

**顺带抓到一条假日志**: 让出时 `_release_camera()` 仍然打
"长时间不在判定区间, 已释放摄像头" —— 原因写错了。日志正是"该让出没让出"这类问题的
第一现场, **假原因比没有原因更糟**(会把人引向错误的方向)。已改成让出时不出声
(`_track_yield` 已经把真正的原因写清楚了), 并加了回归测试
`test_release_log_does_not_lie_about_the_reason`。

## 八、目录速查

```
config.toml                 你的作息/白黑名单/提醒/隐私(每分钟热重载)
calibration.json            你的个人阈值(不要手改, 重跑校准)
data/events.sqlite3         事件流 + 每分钟人脸覆盖率
data/reports/YYYY-MM-DD.md  每日日报
captures/                   分心时的"屏幕+摄像头"拼接图(7 天自动清)
scripts/run.py              试跑入口(带终端输出)
scripts/run_tray.py         日常入口(托盘)
scripts/autostart.py        开机自启 安装/卸载/检查
scripts/calibrate.py        重新校准
scripts/pick_camera.py      选摄像头(人眼确认)
scripts/tune_distance.py    距离/取景调试器
scripts/probe.py            实测工具(list/bench/faces/res/pipe/contend/offline)
scripts/check_face_model.py 人脸模型自检(用标准人像验证模型本身)
tests/                      305 项纯逻辑单测, 不需要摄像头
```
