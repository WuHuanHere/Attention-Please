# attention_please 运行时依赖(由 pip freeze 锁定, 2026-09-17)
# 安装: .\.venv\Scripts\python.exe -m pip install --no-cache-dir -r requirements.txt
# ⚠️ 必须加 --no-cache-dir: 本机默认 pip 缓存目录在工作区之外, 受限环境下会让 pip 挂死不下载。
mediapipe==1.0.1
opencv-contrib-python==5.0.0.93
numpy==2.4.6
pillow==12.3.0
pywin32==312
pystray==0.19.5

# mediapipe 的传递依赖(显式写出以便复现, 版本按 mediapipe 1.0.1 的声明)
absl-py==2.5.0
certifi==2026.7.22
flatbuffers==25.12.19
matplotlib==3.11.2
sounddevice==0.5.6
